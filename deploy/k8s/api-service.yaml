apiVersion: v1
kind: Service
metadata:
  name: studiory-api
  labels:
    app: studiory-api
spec:
  type: ClusterIP
  selector:
    app: studiory-api
  ports:
    - name: http
      port: 80
      targetPort: http
